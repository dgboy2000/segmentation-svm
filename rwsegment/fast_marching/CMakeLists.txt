PROJECT(FASTM)
CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

set(CMAKE_LIBRARY_OUTPUT_DIRECTORY libs)
set(CMAKE_RUNTIME_OUTPUT_DIRECTORY libs)

IF(WIN32)
  add_definitions( -DOSWIN )
ENDIF(WIN32)

add_subdirectory(fastm)
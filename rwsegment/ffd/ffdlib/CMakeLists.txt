PROJECT(FFDLIB)
CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

IF(WIN32)
  add_definitions( -DOSWIN )
ENDIF(WIN32)


ADD_LIBRARY( ffdlib SHARED
    ffdlib.cpp
  )
  
set_target_properties(ffdlib PROPERTIES
    LIBRARY_OUTPUT_DIRECTORY ${PROJECT_BINARY_DIR}/libs)